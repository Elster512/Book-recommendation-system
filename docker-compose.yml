version: "3"
services:
  back:
    image: elster512/bookapiback
    volumes:
      - "data:/app/data"
    ports:
      - 8008:8008
  front:
    depends_on:
      - back
    image: elster512/bookapifront
    ports:
      - 3000:3000
volumes:
  data:
